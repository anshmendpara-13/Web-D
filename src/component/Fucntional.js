import React from 'react';
import "../gharnicss.css"

const Functional = ({ name }) => {
    return (
        <div>
            <h1 className='aquacolor'>Hello, {name}</h1>
        </div>
    );
};

export default Functional;

